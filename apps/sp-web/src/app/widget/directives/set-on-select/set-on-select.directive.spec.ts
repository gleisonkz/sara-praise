import { SetOnSelectDirective } from './set-on-select.directive';

describe('SetOnSelectDirective', () => {
  it('should create an instance', () => {
    const directive = new SetOnSelectDirective();
    expect(directive).toBeTruthy();
  });
});

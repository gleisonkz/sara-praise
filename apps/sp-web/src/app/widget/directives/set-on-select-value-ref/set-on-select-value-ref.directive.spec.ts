import { SetOnSelectValueRefDirective } from './set-on-select-value-ref.directive';

describe('SetOnSelectValueRefDirective', () => {
  it('should create an instance', () => {
    const directive = new SetOnSelectValueRefDirective();
    expect(directive).toBeTruthy();
  });
});
